{
  "name": "@stellarflow/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Typed client SDK for the StellarFlow payment, escrow and registry contracts",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^12.3.0",
    "@stellarflow/config": "workspace:*",
    "@stellarflow/types": "workspace:*"
  },
  "devDependencies": {
    "@stellarflow/test-utils": "workspace:*",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
